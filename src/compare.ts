/** возвращает булевый ответ равны ли параметры firstText и secondText. */
export function isEqual(firstText: string, secondText: string): boolean{
    if (firstText == secondText) return true;
    else return false;
}